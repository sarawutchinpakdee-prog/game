rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /tourism_districts/{doc} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    match /board_cells/{doc} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
