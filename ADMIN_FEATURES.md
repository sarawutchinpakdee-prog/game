# Admin Panel Updates

เวอร์ชันนี้เพิ่มความสามารถให้หน้า Admin จัดการข้อมูลช่องได้ครบขึ้น

## สิ่งที่เพิ่ม

- แก้ `effect_value` ได้จากหน้า Admin
  - ใช้ค่าบวกสำหรับรับเงิน
  - ใช้ค่าลบสำหรับเสียเงิน
- แก้ `effect_steps` ได้จากหน้า Admin
  - ใช้กับช่อง `FORWARD` / `BACKWARD`
  - จำกัดค่าไว้ไม่เกิน 24 ช่องต่อเอฟเฟกต์
- อัปโหลดรูป PNG, JPG/JPEG หรือ WebP ได้โดยตรง
  - จำกัดขนาด 5 MB ต่อไฟล์
  - ไฟล์จะถูกเก็บใน `public/uploads/`
  - ระบบตรวจชนิดไฟล์จาก header จริงก่อนบันทึก
- หลังอัปโหลด ระบบจะใส่ URL รูปให้ในช่อง `image_url` อัตโนมัติ
- ปรับการแสดงชื่อ/URL บนกระดานให้ไม่ใช้ `innerHTML` กับข้อมูลจากฐานข้อมูลโดยตรง
- เพิ่ม validation ฝั่ง Server สำหรับประเภทช่อง ราคา ค่าเช่า ค่าผลกระทบ จำนวนช่องเดิน และ URL รูปภาพ

## การใช้งานอัปโหลดรูป

1. เข้าหน้า Admin
2. คลิกช่องที่ต้องการแก้ไข
3. กดเลือกไฟล์ในหัวข้อ `รูปภาพ`
4. กด `⬆️ อัปโหลด`
5. ตรวจดู Preview
6. กด `บันทึก`

รองรับเฉพาะ PNG, JPG/JPEG และ WebP ขนาดไม่เกิน 5 MB
